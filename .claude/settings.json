{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE 'git push.*\\b(master|main)\\b' && { echo 'Pushing to master is blocked' >&2; exit 2; } || exit 0"
          }
        ]
      }
    ]
  }
}